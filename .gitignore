# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Test / type-check / coverage caches
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# QueryX database artifacts produced at runtime (data files, WAL logs)
*.qx
*.db
*.wal
*.log

# QueryX runtime database directories (catalog + page/WAL files) created by the
# shell/demos; these are data, not source.
mydb/
queryx_data/
catalog.json

# Local Claude Code settings (machine-specific; not shared)
.claude/settings.local.json

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
